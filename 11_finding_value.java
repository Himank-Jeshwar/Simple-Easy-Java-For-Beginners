class Calculate
{
    public static void main(String[] args) 
    {
        //given numbers
        int a = 2;
        int b = 8;
        //calculating
        double x = (2*Math.pow(a,5)/3*a*a*b)+3*Math.pow(b,7);
        //output
        System.out.println("Value of x = "+x);    
    }
}